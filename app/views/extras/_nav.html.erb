<nav class="navbar navbar-light bg-primary">
  <%= link_to 'RMIT MATCHWORKS', root_path, class: "navbar-brand" %>
    <div class="custom-btn">
        <% if user_signed_in? %>
          <%= "Welcome to Matchworks, #{current_user.name}!" %>
          <%= link_to("Log Out", destroy_user_session_path, class:"btn btn-primary")%>
          <%= link_to "Profile", profile_pages_main_path, class:"btn btn-primary" %>
        <% else %>
          <!--%= "Looking to work? ->" %-->
          <%= link_to "Sign Up", new_user_registration_path, class:"btn btn-primary"%>
          <%= link_to "Sign In", new_user_session_path, class: "btn btn-primary"%>
        <% end %>
    </div>
</nav>
<!-- end of Nav -->
<%= render 'extras/notice'%>
